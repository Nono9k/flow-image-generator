import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    name: "Flow Image Generator",
    description: "Asset-based batch image generation for Google Flow.",
    version: "0.2.0",
    permissions: ["downloads"],
  },
});
